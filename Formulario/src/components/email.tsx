export function Email() {
  return (
    <div>
      <input
        type="email"
        name="email"
        value="email"
        placeholder="email"
        required
      />
    </div>
  );
}
